# Eval — эталонный список находок

Фикстура: `eval/fixture-site/` (index.html, services.html, team.html, landing.html, es/index.html, sitemap.xml).
Входной файл для прогона: `eval/Сайты.eval.txt`.

**Важно:** эта фикстура НЕ претендует на полное прохождение всего чек-листа (нет favicon, нет og:-тегов, Schema есть только на index.html/es/index.html/landing.html и умышленно неполная и т.п.) — сайт умышленно минималистичный, чтобы не размывать фокус. Сверяйте отчёт агента ТОЛЬКО по 37 пунктам ниже. Любые другие мелкие находки сверх этого списка (например, «нет favicon») — ожидаемы и не считаются провалом eval.

Каждый пункт — это конкретная регресс-опасная зона, которая уже реально ломалась/уточнялась в истории этого проекта (см. подпись «почему важно»).

## Должны быть найдены (Баги)

1. **[technical-checks §1, index.html]** Реальная console-ошибка от собственного скрипта сайта (`Uncaught TypeError ... at estimator.js:42`).
   Почему важно: это позитивный кейс фильтра консоли — агент обязан ЛОВИТЬ ошибки собственного кода, фильтр не должен глушить всё подряд.

2. **[technical-checks §7, services.html vs index.html]** Разные номера телефона в шапке (`+1 (555) 555-0100` vs `+1 (555) 555-0199`) на двух страницах одного DEV-сайта без контекстного объяснения.
   Почему важно: было реальное исправление логики свопов в этом проекте — регресс здесь означает, что правило DEV="один номер everywhere" снова сломано.

3. **[technical-checks §6, landing.html]** Страница объявлена как лендинг в `Лендинги:`, но нет `noindex`.
   Почему важно: это единственный способ проверить, что связка «Лендинги: → обязательный noindex» всё ещё работает.

4. **[visual-checks §4, services.html vs index.html]** H1 дословно совпадает («Professional Moving Services in New York») на двух разных страницах одного типа.
   Почему важно: базовая проверка чек-листа, sanity-check что общий список не сломан попутными правками.

5. **[content-checks §1, es/index.html]** Футер остался непереведённым (английский текст на испанской версии) — X-site баг (переиспользуемый компонент).
   Почему важно: проверяет, что мультиязычная логика (десктоп=основной язык / мобилка=вторая версия) реально доходит до контентной проверки конкретной страницы.

6. **[CLAUDE.md Шаг 0 — карта полей, index.html форма]** Поле First Name визуально помечено звёздочкой как обязательное, но у `<input>` нет атрибута `required`.
   Почему важно: прямая проверка «карты полей» — визуальная пометка vs реальный `required`, добавленной как отдельный обязательный шаг перед сабмитом.

7. **[form-submit-checks — техническая верификация сабмита, index.html форма]** При клике Submit форма визуально показывает «Спасибо!», но не отправляет НИ ОДНОГО сетевого запроса.
   Почему важно: это ключевой новый критерий («визуальный результат недостаточен, нужен сетевой запрос» / «нет запроса = провал») — самая свежая и самая непроверенная в бою часть промпта.

8. **[form-submit-checks, калькулятор — п.5, index.html]** Разбивка показывает «База: $50» + «Расстояние: $40», но «Итого: $115» (сумма частей 90 ≠ итог 115). С 2026-09-23 в `Сайты.eval.txt` (`Комментарии:`) передано ТЗ на формулу (`cost = base_rate + distance_charge`) — это больше не случай «ТЗ нет», а случай с явным эталоном.
   Почему важно: проверка 5-го критерия калькулятора (внутренняя арифметическая согласованность) — и, с появлением ТЗ, заодно проверка нового формата «Доказательство» (см. CLAUDE.md, «Что реально печатается в отчёт»): раз эталон передан явно, доказательство должно быть оформлено как **Ожидалось: $90 (по ТЗ, $50 + $40) / Получено: $115**, а не прозаическим пересказом. Если агент оформил это прозой, не разделив на два значения — регресс нового правила Ожидалось/Получено.

9. **[content-checks §1, index.html]** Абзац lorem ipsum.
   Почему важно: базовый sanity-check, что фундаментальные content-проверки не задеты попутными правками.

10. **[technical-checks §7, team.html]** «Личный» номер Alex Rivera (`+1 (555) 555-0175`, подписан «Alex's direct line») — на той же странице чуть ниже тот же самый номер подписан «General support line».
    Почему важно: реально пойманная в бою нестыковка — в одном прогоне на клиенте агент классифицировал внешне похожий случай (номер сотрудника, отличающийся от сайт-вайд) как «контекстно оправданное персональное исключение» из `technical-checks` §7, хотя при более внимательной проверке оказалось, что это не уникальная личная линия, а просто переиспользуемый номер. Одинаковый номер с двумя разными подписями на одной странице — проверяемый факт, доказывающий, что это НЕ уникальная личная линия, а баг (устаревший/неверно помеченный номер), даже без внешнего сайта для сравнения. Правильное поведение: зафиксировать как баг (🟥, `[Technical]`, NAP), а не пропустить как исключение.

## НЕ должны быть найдены (ловушки на ложные срабатывания)

11. **[technical-checks §1, index.html]** Console-ошибка/failed request от `beacon.cdn-nitrooptimize.io` (фейковый сторонний телеметрический домен).
    Почему важно: это ровно тот кейс (NitroPack 403), из-за которого в этом проекте появилось правило «молча игнорировать сторонний телеметрический шум». Если агент это упомянул — фильтр слишком узкий/сломан.

12. **[technical-checks — ADA exclusion, index.html]** Иконка-кнопка (✕) без текста/`aria-label`, но полностью кликабельна и функциональна — чисто ADA-проблема (нет альтернативного текста для скринридера).
    Почему важно: проверяет, что явное исключение ADA из скоупа агента реально соблюдается, а не подразумевается только в описании.

13. **[technical-checks §8 / hreflang-checks, index.html / es/index.html]** Отсутствие hreflang-тегов в `<head>` — агент должен явно НАЗВАТЬ это отсутствующим методом (а не промолчать и не сделать общий вывод «мультиязычность работает»/«не работает» без разбивки по методам). Content-Language заголовок и hreflang-аннотации в sitemap физически нельзя проверить на статической файловой фикстуре без сервера — если агент напишет «не проверено технически в этом прогоне» по этим двум методам (как и предписывает SKILL.md), это правильное поведение, а не пропуск.
    Почему важно: проверяет, что для каждого из трёх методов даётся явный вердикт присутствует/отсутствует/не проверено, а не расплывчатое обобщение; заодно проверяет, что `hreflang-checks` реально вызывается на этой мультиязычной фикстуре (а не пропускается по ошибке как «одноязычный сайт»).

## Поведенческие проверки (новая логика `form-submit-checks` и подсчёта sitemap, не баги контента)

14. **[form-submit-checks, services.html «Request a Callback» vs index.html «Get a Free Quote»]** Обе формы визуально разные (разный заголовок/CTA/набор полей), но несут одинаковый скрытый `<input type="hidden" name="_form_template" value="TPL-QUOTE-01">`.
    Должно произойти: агент обязан распознать по техническому ID, что это один и тот же переиспользуемый шаблон, и **не сабмитить «Request a Callback» повторно** как отдельную форму — только явно указать в отчёте, что шаблон уже протестирован (на index.html) и это тот же ID. Если агент протестировал обе формы независимо, как будто это два разных шаблона — регресс дедупликации по техническому ID (приоритет ID > набор полей > визуал). Дополнительно можно свериться с файловой системой: после прогона должен появиться файл `fixture-site/{прогон}/.form-submissions/TPL-QUOTE-01.json` (или аналогично поименованный, `{прогон}` — подпапка с датой/временем старта этого прогона, см. CLAUDE.md, «Рабочая папка сайта») — если агент вообще пытался использовать механизм маркер-файла.

15. **[form-submit-checks, index.html «Subscribe to our newsletter»]** Форма подписки на рассылку — не лид-генерирующая.
    Должно произойти: агент обязан протестировать её **облегчённым протоколом** — один валидный сабмит с технической верификацией сети (реальный `fetch`-запрос на `example.invalid/newsletter-subscribe` должен быть найден в `playwright-cli requests`), без граничного/невалидного сабмита, без троттлинг-теста, без применения потолка в 10 попыток. Если агент вообще не протестировал эту форму (старое поведение «второстепенные формы не сабмитим») — регресс: правило «все формы тестируются, просто с разной глубиной» перестало соблюдаться. Если агент применил к ней полный лид-генерирующий протокол (граничный кейс и т.п.) — тоже регресс, просто в другую сторону (классификация лид-ген/не-лид-ген перепутана).

16. **[form-submit-checks, services.html «Leave a Review»]** Форма отзыва с явным текстом «published immediately... no moderation queue».
    Должно произойти: агент обязан **НЕ сабмитить** эту форму автоматически, а зафиксировать в отчёте, что найдена форма, публикующая контент немедленно без модерации, и что нужно отдельное явное подтверждение пользователя перед её тестированием. Если агент её всё же засабмитил — прямой регресс правила-исключения (публикация публичного контента не покрывается общим согласием на тестирование форм).

17. **[CLAUDE.md Шаг 1, sitemap.xml]** `sitemap.xml` в корне фикстуры содержит ровно 5 записей `<loc>` (index.html, services.html, team.html, landing.html, es/index.html).
    Должно произойти: агент обязан посчитать и явно указать в шапке отчёта «Всего URL по sitemap: 5» (через `curl`/эквивалент по сырому XML, не через браузер). Если поле в отчёте отсутствует вовсе, или число неверное, или агент попытался посчитать страницы обходом сайта по ссылкам вместо чтения sitemap — регресс.

## Дополнительно: своп телефонных номеров на LIVE

Фикстура: `eval/fixture-site/swap-live.html`. Входной файл: `eval/Сайты.eval-live.txt` (отдельный от основного — здесь окружение LIVE, а не DEV). Прогонять и сверять так же, как основную фикстуру (см. `eval/README.md`), просто с другим входным файлом.

Оба пункта добавлены не гипотетически, а по мотивам реального промаха, пойманного на живом клиенте (apexwindowwerks.com, 2026-09) — расхождение `href`/`aria-label` кнопки звонка, которое на самом деле оказалось переходным состоянием асинхронного свопа, а не багом.

18. **[technical-checks §7, swap-live.html]** Кнопка звонка: `href` в исходном HTML — `tel:+15555550199`, `aria-label` — «Call (555) 555-0100» (не совпадают). Через ~3 секунды скрипт на странице меняет `href` на `tel:+15555550100` — теперь совпадает.
    Должно произойти: **НЕ баг**, если агент подождал стабилизации (5–10 секунд, см. правило в начале §7) и перечитал значение перед сравнением. Если агент зафиксировал расхождение по значению сразу после открытия страницы, не подождав — это регресс: правило ожидания перестало применяться.

19. **[technical-checks §7, swap-live.html]** Console-ошибка `Uncaught TypeError: swap is not a function at callrail.js:17` — от собственного скрипта свопа (не сторонняя телеметрия, не фильтруется).
    Должно произойти: **баг**, зафиксированный по умолчанию (окружение LIVE). Если находки нет — регресс: проверка технической отработки свопа перестала быть default-поведением на LIVE (могла случайно снова стать opt-in-only, как было до объединения `phone-swap-checks` в `technical-checks`).

## Дополнительно: Schema (JSON-LD) и rel/nofollow (2026-09-17)

Добавлено по мотивам реального разрыва, найденного при сверке отчёта агента с ручным ClickUp-чеклистом для реального клиента (hhs---dev.webflow.io, 2026-09-17): дедупликация Schema, `sameAs`, обязательность Product на сервисных страницах и rel/nofollow-политика существовали в промпте, но систематически не срабатывали на практике.

20. **[technical-checks §9 Шаг 1, index.html]** Блок `LocalBusiness` JSON-LD задублирован целиком — два идентичных `<script type="application/ld+json">` в `<head>` (типичный copy-paste баг).
    Почему важно: проверяет, что подсчёт блоков по `@type` реально выполняется ПЕРВЫМ шагом, до разбора полей — именно в такой последовательности этот дубль был пропущен в реальном отчёте на клиенте, хотя правило дедупликации формально уже существовало.

21. **[technical-checks §9, index.html]** `LocalBusiness` JSON-LD присутствует, но без `sameAs` — при этом в футере той же страницы есть реальные собственные ссылки клиента на соцсети (Facebook).
    Почему важно: `sameAs` — агентство-обязательное поле (не Google-минимум), отсутствие которого должно попадать в «Баги», а не в «Рекомендации» — именно эта путаница уровней обязательности была реальным пропуском в отчёте на клиенте.

22. **[technical-checks §9, services.html]** Сервисная страница (`services.html`) не содержит вообще никакой Schema-разметки, в том числе `Product`.
    Почему важно: по стандарту агентства `Product` ожидается на сервисных страницах по умолчанию, не опционально — в реальном отчёте на клиенте отсутствие `Product` на сервисных страницах не было упомянуто вообще ни разу.

23. **[technical-checks §5, index.html]** Два независимых нарушения rel/nofollow-политики в одном месте: ссылка на собственный Facebook клиента (footer) помечена `rel="nofollow"` (должна быть dofollow — это ресурс самого клиента), а ссылка на внешний государственный реестр (NY State Movers Registry) вообще без `nofollow` (должна быть nofollow — это не ресурс клиента).
    Почему важно: реальная политика («dofollow только для ресурсов самого клиента, nofollow для всех остальных внешних ссылок») была уточнена именно по мотивам похожего разрыва на реальном клиенте — этот пункт проверяет оба направления ошибки сразу, не только одно.

## Дополнительно: мультиязычная адаптация Schema и полный инвентарь интерактива (2026-09-18)

Добавлено по мотивам двух новых правил, уточнённых в промпте без ещё ни одного реального прогона на клиенте — по собственному критерию фикстуры («проверки со сложной условной логикой» — см. `eval/README.md`) это достаточное основание добавить регресс-тест заранее, не дожидаясь реального промаха.

24. **[technical-checks §9, index.html]** Оба (задублированных) блока `LocalBusiness` содержат `"inLanguage": "en"` — голый код языка вместо требуемого полного регионального (`"en-US"`).
    Почему важно: проверяет, что агент требует именно региональный код, а не любой код языка вообще — это разрешает голому `"en"`/`"es"` тихо проскочить как «вроде похоже на правильно».

25. **[technical-checks §9, index.html]** Оба блока `LocalBusiness` содержат `"knowsLanguage": ["en-US"]` — сайт двуязычный (EN+ES, см. `es/index.html`), но испанский язык в списке не упомянут.
    Почему важно: проверяет, что `knowsLanguage` сверяется на ПОЛНОТУ (все реально поддерживаемые сайтом языки), а не только на факт присутствия поля.

26. **[technical-checks §9, es/index.html]** `LocalBusiness` JSON-LD на испанской версии присутствует, но `inLanguage` отсутствует вообще.
    Почему важно: проверяет, что `inLanguage` реально проверяется на ОБЕИХ языковых версиях сайта, а не только на основной (английской) — именно эту границу уточняли последней.

27. **[CLAUDE.md Шаг 3.2 / visual-checks §9, services.html]** FAQ-аккордеон из двух вопросов, не упомянутый нигде в меню/навигации; у второго вопроса `onclick` ссылается на несуществующий id панели — клик по кнопке визуально ничего не делает.
    Почему важно: проверяет одновременно две вещи — (а) что агент вообще НАШЁЛ этот виджет через целенаправленный инвентарь интерактива, а не только то, что попалось по пути (на странице нет ни одной подсказки про FAQ, кроме самого блока); (б) что каждый ИЗ ДВУХ вопросов реально кликается, а не только первый/визуально похожий.

28. **[technical-checks §9 FAQPage, services.html]** На той же странице виден Q&A-блок (см. п.27), но JSON-LD разметки `FAQPage` для него нет.
    Почему важно: ловушка на ложное срабатывание — отсутствие `FAQPage` при видимом Q&A НЕ баг, только рекомендация добавить (влияет на SERP-сниппет, не обязательно) — если агент зафиксировал это как баг или вообще не упомянул, это регресс в любую сторону.

## Дополнительно: форматирование телефона, Schema на лендингах, ленивые отзывы, новая структура отчёта (2026-09-18)

Добавлено по мотивам правки промпта, сделанной по прямому запросу пользователя (не по промаху на клиенте, но затрагивает ровно ту «условную логику», которую правило роста фикстуры просит покрывать заранее — см. `eval/README.md`, критерий №1): (а) отчёт теперь состоит из 6 блоков вместо 3 — SEO/rel/target/Schema переехали из «Багов» в свои отдельные блоки со своей приоритетностью; (б) консистентность телефона сравнивает только цифры, не форматирование; (в) Schema на лендингах не проверяется вовсе; (г) Review/AggregateRating может подгружаться вместе с виджетом отзывов позже основного контента.

29. **[technical-checks §7, landing.html]** Footer landing.html повторяет тот же канонический DEV-номер, что в шапке (`+15555550100`), но другим форматированием — `555-555-0100` вместо `+1 (555) 555-0100` (без `+1`, без скобок, дефисы вместо пробела).
    Должно произойти: **НЕ баг** — после нормализации (отбросить нецифровые символы и код страны) цифры совпадают. Если агент зафиксировал это как расхождение номера — регресс правила «сравнивай только цифры, не форматирование» (`technical-checks` §7).

30. **[technical-checks §9, landing.html]** На лендинге умышленно лежит явно битый `LocalBusiness` JSON-LD (чужое название бизнеса «Totally Different Movers LLC», чужой телефон) — ровно тот тип находки, который на обычной странице сайта был бы однозначным багом («данные от другого бизнеса»).
    Должно произойти: **содержимое блока (чужое название/телефон) не должно быть проанализировано или упомянуто нигде в отчёте** — ни как баг, ни как рекомендация. Нейтральная строчка о границах скоупа («Schema на лендингах не проверяется, landing.html не включён в этот раздел») — это НЕ регресс, это нормальная и ожидаемая практика объяснять скоуп проверки; такая заметка не раскрывает и не анализирует само содержимое блока, поэтому допустима. Регресс — это конкретно появление деталей содержимого (названия «Totally Different Movers LLC», телефона, факта дублирования/нерелевантности данных) где-либо в отчёте.

31. **[technical-checks §9, index.html]** Виджет «What Our Customers Say» рендерит контент и добавляет в DOM `Product`/`Review`/`AggregateRating` JSON-LD только через ~2 секунды после загрузки страницы (типичный паттерн ленивого виджета отзывов вроде Elfsight/Trustpilot) — при первом извлечении JSON-LD сразу после навигации этого блока ещё не существует.
    Должно произойти: **НЕ баг**, если агент прокрутил к виджету отзывов / дождался его прогрузки и повторно извлёк JSON-LD перед выводом о находках (см. `technical-checks` §9, пункт про `Review`/`AggregateRating` и асинхронную подгрузку). Если агент зафиксировал «Product/Review/AggregateRating отсутствует на index.html», не дождавшись виджета, — регресс.

## Дополнительно: JS-маска телефона — граничный кейс разными способами ввода (2026-09-21)

Добавлено по мотивам реального промаха на живом клиенте (bcm-11f3s9g39fk4.wpf.comradeserver.com, 2026-09-21): агент (и подагент) зафиксировали Critical-баг «12-значный номер молча обрезается и уходит в CRM искажённым», используя программную подстановку значения поля (`fill()`/`eval` на `.value`) вместо настоящего посимвольного ввода. Пользователь лично воспроизвёл тот же сценарий настоящей вставкой (Ctrl+V) — форма корректно заблокировала сабмит. Причина расхождения: маскирующий скрипт вёл внутреннее состояние («сколько цифр реально введено»), обновлявшееся только на настоящих keydown-событиях — при подстановке значения целиком это состояние осталось пустым/устаревшим. Первая версия этой ловушки в фикстуре ошибочно ломала маску на ЛЮБОЙ массовой подстановке значения, независимо от длины — уточнена так, чтобы граница была именно про длину значения, а не про способ ввода вообще (см. ниже), иначе тестирование только вставкой валидного номера тоже "прошло бы" не вскрыв ничего.

32. **[CLAUDE.md «Обработка ошибок» — граничные значения разными способами ввода, index.html, поле Phone в форме «Get a Free Quote»]** У поля `#phone` есть JS-маска с внутренним счётчиком цифр (`raw`), обновляемым только настоящими `keydown`-событиями. Три способа ввода дают три разных результата:
    - **Настоящий посимвольный ввод** (по одной клавише) больше 10 цифр → безопасно ограничивается 10 цифрами, `(XXX) XXX-XXXX`, лишние цифры молча игнорируются.
    - **Массовая подстановка/вставка НОРМАЛЬНОЙ длины** (≤10 цифр, например настоящая вставка уже валидного номера) → тоже корректно маскируется — это НЕ баг, тестирование только вставкой обычного номера ничего не вскроет.
    - **Массовая подстановка/вставка ДЛИННЕЕ 10 цифр** (`fill()`, `eval` на `.value`, вставка сразу 12 цифр) → намеренный баг фикстуры: молча принимает сырые неотформатированные 12 цифр без какой-либо валидации (`checkValidity()` вернёт `true`).
    Должно произойти: **НЕ баг**, если агент тестировал именно граничный (>10 цифр) кейс настоящим посимвольным вводом — тогда расхождения не будет вообще. Регресс — если агент (или параллельный подагент) протестировал ГРАНИЧНЫЙ кейс только через `fill()`/`eval`-подстановку значения целиком (или только вставкой без реальных событий), увидел сырые 12 цифр без ошибки валидации и зафиксировал это как подтверждённый баг, **не перепроверив тот же граничный кейс настоящим вводом/вставкой** — это ровно тот регресс, который поймал реальный клиент: правило «граничные значения маскированных полей тестируем несколькими реалистичными способами ввода, не полагаемся на один способ» (см. `form-submit-checks`, «Граничные значения», и `CLAUDE.md`, «Обработка ошибок») не соблюдено.

## Дополнительно: горизонтальный скролл, три категории rel/target, нумерация сабмитов (2026-09-22)

Добавлено по мотивам реальных промахов на живом клиенте (bestcaliforniamovers.com, 2026-09-22): (а) агент принял горизонтально-скроллируемую карусель бейджей за обрезанный контент, проверив только геометрию (`getBoundingClientRect`), не проверив скроллируемость; (б) при разборе rel/target-атрибутов агент перепутал три независимые категории ссылок (внутренние / свои ресурсы клиента вроде Google Maps / прочие внешние), из-за чего некорректно классифицировал находки. Заодно добавлена поведенческая проверка новой сквозной нумерации тестовых сабмитов (`form-submit-checks`).

33. **[visual-checks §1 / CLAUDE.md «Обработка ошибок», team.html]** Ряд бейджей сертификатов (`.badge-row`) — `overflow-x:auto`, `scrollWidth` больше `clientWidth` контейнера, карточка «BBB A+» не помещается в исходный вьюпорт без скролла.
    Должно произойти: **НЕ баг**, если агент проверил `overflow-x`/`scrollWidth` через `eval` и реально проскроллил контейнер (`el.scrollLeft = el.scrollWidth`) — карточка отображается целиком. Если агент зафиксировал это как обрезку/наезжающие элементы, не проверив скроллируемость контейнера, — регресс.

34. **[technical-checks §5, team.html]** Три ссылки одновременно: «Get Directions» — ссылка на Google Maps клиента, `target="_blank"` без `rel` вообще; «See Our Services» — внутренняя ссылка (не файл) с `target="_blank"`; «Moving Industry Association» — прочая внешняя (не клиента) ссылка с `target="_blank"` без `rel` вообще.
    Должно произойти: агент обязан развести это на три РАЗНЫХ находки по правильным критериям — (1) «Get Directions»: отсутствие `noopener`/`noreferrer` здесь КОРРЕКТНО (это ресурс клиента) — не баг; (2) «See Our Services»: баг оси target — внутренняя ссылка не должна открываться в новой вкладке (🟨 Major); (3) «Moving Industry Association»: баг оси target/noopener — реальный риск tab-nabbing на внешнем ресурсе не клиента (🟥 Critical). Если агент зафиксировал (1) как баг («не хватает noopener»), или не заметил (2)/(3), или смешал все три в одну находку — регресс: правило «target=_blank решается ролью ссылки (внутренняя / своя-клиента / прочая внешняя), не единым правилом для всех» перестало соблюдаться.

35. **[form-submit-checks, index.html + services.html, поведенческая проверка]** Реальные валидные сабмиты за этот прогон (с учётом дедупликации по техническому шаблону, см. п.14 — «Get a Free Quote»/«Request a Callback» это один шаблон, «Subscribe to our newsletter» — отдельный) должны получить сквозные номера 1, 2… на весь прогон, а не свои для каждой формы.
    Должно произойти: в тестовых данных валидного сабмита — суффикс `-N` в фамилии и последние 2 цифры телефона `01`, `02`… по номеру сабмита; в отчёте — таблица «Что было отправлено в тестовых формах» с этими номерами и thank-you/результатом каждого. Если агент не пронумеровал сабмиты сквозным счётчиком (например, использовал случайные последние цифры телефона в каждом) или не привёл такую таблицу — регресс новой инструкции («Сквозная нумерация сабмитов» в `form-submit-checks`).

## Дополнительно: NAP — консистентность адреса (2026-09-23)

Добавлено по прямому запросу пользователя (не по промаху на клиенте) — покрывает новую условную логику `technical-checks` §7 «NAP», расширенного с телефона на Name/Address: сравнение адреса по смыслу после нормализации сокращений, а не по строке, и то, что в отличие от телефона деление DEV/LIVE для адреса не действует вовсе (canonical-адрес зашит в Schema `index.html` и в новом видимом футере/блоке «Find Us», см. `team.html`/`services.html`).

36. **[technical-checks §7 Address, team.html vs index.html]** Footer index.html показывает «123 Example Ave, New York, NY 10001» (совпадает со Schema-адресом), блок «Find Us» на team.html показывает «123 Example Avenue, New York, NY 10001» — тот же адрес, другое написание (`Ave` vs `Avenue`, лишняя запятая).
    Должно произойти: **НЕ баг** — после нормализации сокращений (см. `technical-checks` §7, Address) это один и тот же адрес. Если агент зафиксировал это как NAP-расхождение — регресс правила «сравнивай адрес по смыслу, не по строке».

37. **[technical-checks §7 Address, services.html vs index.html]** Footer services.html показывает «456 Different Rd, New York, NY 10001» — другая улица и номер дома, чем канонический «123 Example Ave, New York, NY 10001» на index.html/Schema.
    Должно произойти: **баг** (🟥, `[Technical]`, NAP) — реальное расхождение адреса, не объяснимое форматированием. Если агент не нашёл расхождение, или захеджировал его («возможно легитимно») — регресс: сайт-вайд сравнение адреса между DEV-страницами не требует внешнего эталона (в отличие от телефона, деление DEV/LIVE тут не действует).

## Дополнительно: новый формат карточки бага — тег типа, обязательное доказательство, без советов (2026-09-23)

Добавлено по прямому запросу пользователя, не привязано к содержимому конкретной страницы — это требование к СТРУКТУРЕ каждой карточки в разделе «Что нужно доделать», проверяй его на ВСЕХ найденных багах в отчёте (пункты 1–10, 19, 27, 36–37 и любые другие), не только на новых пунктах выше.

Должно быть верно для каждой карточки бага без исключений (см. CLAUDE.md, «Что реально печатается в отчёт»):
- Заголовок начинается с тега `[Form]`/`[Technical]`/`[Visual]`/`[Interactive]`/`[SEO]`/`[Rel/Target]`/`[Schema]`, соответствующего типу находки (например, пункт 1 — `[Technical]`, пункт 4 — `[Visual]`, пункт 7 — `[Form]`, пункт 3 — `[SEO]`). Тег отсутствует, стоит не в начале заголовка, или не совпадает с типом находки — регресс.
- У карточки есть поле «Доказательство» с реальным подтверждением (встроенный скриншот или дословный технический факт), а не только пересказ в «Что происходит». Карточка без отдельного доказательства — регресс.
- Нигде в отчёте нет поля «Рекомендация для DEV» и нет советов/гипотез о причине бага или о том, как его чинить — агент только фиксирует и подтверждает баг. Появление совета по фиксу («похоже на...», «стоит проверить...», «причина, вероятно, в...») — регресс.
- Соседние карточки багов разделены горизонтальной чертой `---`.
- Пункт 8 (арифметика калькулятора) — с 2026-09-23 в `Сайты.eval.txt` передано ТЗ на формулу, поэтому доказательство должно быть оформлено как «Ожидалось: $90 / Получено: $115», а не прозаическим пересказом суммы. Оформление прозой без явного разделения на два значения — регресс нового правила «Ожидалось/Получено» (см. CLAUDE.md).

## Как читать результат
- Пункты 1, 2, 4, 5, 6, 7, 8, 9, 10, 19, 27, 37: должны появиться в «Баги» отчёта. Отсутствие любого — регресс, нужно найти, какая правка его вызвала (`git log` / `git diff` по соответствующему SKILL.md или CLAUDE.md). Пункт 10 дополнительно обязан попасть именно в 🟥 (`[Technical]`, NAP), а не в «Рекомендации» — если агент захеджировал его как «возможно легитимно», это тоже регресс (см. правило уверенности в CLAUDE.md — здесь есть проверяемый факт, догадка не нужна). Пункт 27 (FAQ-аккордеон) — сверяй ещё и сам факт обнаружения виджета, не только формулировку находки: если агент вообще не упомянул FAQ-блок на `services.html`, это тоже регресс, даже без явного бага в тексте отчёта.
- **Пункт 3 (отсутствующий `noindex` на лендинге) — с 2026-09-22 отчёт не делится на блоки по типу (см. CLAUDE.md «Формат отчёта»), но это по-прежнему SEO-находка по своей логике важности**, а не по общей серьёзности бага: значок и тег `[SEO]` в заголовке карточки должны отражать SEO-критерий (риск попадания в индекс), не тег `[Technical]`/общую формулировку «баг». Сверяй по тегу и значку, не по названию несуществующего блока — и не жди никакого текстового обоснования критерия в самой карточке (см. новую политику «без советов/без рубрики классификации» ниже).
- **Пункт 5 (непереведённый футер на `es/index.html`) классифицируется как обычный визуальный/контентный дефект** (`content-checks` §1, качество перевода), а не как SEO-находка про hreflang. Если агент обосновал её через «метод декларации мультиязычности»/hreflang — регресс: значит, агент спутал «метод декларации мультиязычности» с «качеством самого перевода» — эти два понятия должны быть разведены явно независимо от структуры отчёта.
- **Пункт 29 — не баг** (см. отдельное описание выше), перечислен здесь по формату ссылки на раздел, а не как «должен появиться в Багах».
- **Пункты 20–22, 24–26 (Schema) — конкретные проверяемые факты, классифицируются по Schema-критериям важности** (переспам/невалидность/чужие данные/обязательные поля — см. CLAUDE.md), не по общей серьёзности бага и точно не как «Рекомендации» — формулировка «может быть осознанным решением» сюда не подходит, это не двусмысленные случаи.
- **Пункт 23 (rel/nofollow) классифицируется по критериям оси «nofollow/dofollow»** (передача ссылочного веса — см. CLAUDE.md, `technical-checks` §5), не по общей серьёзности бага.
- Пункты 11–13, 28: НЕ должны появиться как баги (11, 12, 28) либо должны быть описаны с явной разбивкой по методам, а не общей фразой (13). Появление 11/12 в отчёте как баг, или появление 28 как бага — тоже регресс (слишком широкий скоуп проверки); пункт 28 при этом ДОЛЖЕН появиться в «Рекомендации» — полное отсутствие упоминания тоже регресс, отличай от «не баг вообще». Если пункт 13 всё же даёт дискретную находку (например, «hreflang в head отсутствует») — она должна быть обоснована как SEO-находка (риск для мультиязычной индексации), не как обычный баг и не расплывчато в тексте без чёткой формулировки.
- Пункты 14–17: поведенческие проверки новой логики форм/sitemap — сверяй не по наличию в «Багах», а по тому, что агент СДЕЛАЛ (сабмитил/не сабмитил/сколько раз/какое число посчитал), см. описание каждого пункта.
- Пункт 18: НЕ должен появиться как баг — если появился, значит правило ожидания стабилизации номера (см. `technical-checks` §7) перестало соблюдаться.
- **Пункт 30 (Schema на лендинге) — содержимое блока молчит** — не баг и не рекомендация про название/телефон/дубли внутри него; нейтральная заметка про сам факт «раздел Schema не заполняется для лендингов» — это НЕ регресс (см. уточнение выше).
- **Пункт 31 (ленивые Product/Review/AggregateRating) — НЕ баг**, если агент дождался виджета; появление как находка «Product/Review/AggregateRating отсутствует на index.html» — регресс.
- **Пункт 32 (JS-маска телефона, fill() vs реальный ввод) — НЕ баг**, если граничный кейс (>10 цифр) тестировался настоящим посимвольным вводом. Появление в отчёте (в любом блоке, включая «Формы и калькуляторы») находки вида «12-значный номер молча принимается/обрезается без валидации» — регресс, если при этом агент не подтвердил то же самое отдельно настоящим посимвольным вводом (а не только `fill()`/`eval`-подстановкой значения).
- **Пункт 33 (скроллируемая карусель бейджей) — НЕ баг**, если агент проверил скроллируемость контейнера и подтвердил, что карточка видна целиком после скролла. Появление как находка про «обрезку»/«наезжающие элементы» без проверки скролла — регресс.
- **Пункт 34 (три категории rel/target) — три отдельные, по-разному классифицированные находки**, не одна: (1) Google Maps клиента без `noopener`/`noreferrer` — это ожидаемо, не баг вообще, не должно попасть в отчёт как проблема; (2) внутренняя ссылка с `target="_blank"` — 🟨 Major, ось target; (3) внешняя не-клиентская ссылка без `rel` при `target="_blank"` — 🟥 Critical, та же ось. Если (1) появилась как находка, или (2)/(3) пропущены, или все три смешаны в одну запись — регресс.
- **Пункт 35 (сквозная нумерация сабмитов) — поведенческая проверка**: сверяй не по «Багам», а по тому, использовал ли агент номер сабмита в тестовых данных (фамилия/телефон) и привёл ли таблицу сабмитов с номерами в отчёте.
